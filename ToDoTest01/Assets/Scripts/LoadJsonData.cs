/*

using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.IO;

public class LoadJsonData : MonoBehaviour
{
    public TaskManager TaskManagerScript;

    // Start is called before the first frame update
    void Start()
    {
        string jsonFileName = "TaskManager.json";
        string filePath = Path.Combine(Application.persistentDataPath, jsonFileName);

        TaskManagerScript.readTaskFromJson(filePath);
    }
}

*/
